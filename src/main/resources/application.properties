spring.datasource.url=
spring.datasource.username=
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform = org.hibernate.dialect.MySQL5Dialect
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto = update

Restructured the project a bit, but it's still work in progress. As for now, it can process JWTs, but it doesn't have the logic implemented to automatically extract JWTs from the requests/responses and act accordingly